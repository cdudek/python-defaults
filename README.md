# python-defaults

### Install dependency

#### With virtualenv

```
python -m venv .venv
.venv/bin/pip install -r requirements.txt
```

#### With conda

```
conda install --yes --file requirements.txt
```
